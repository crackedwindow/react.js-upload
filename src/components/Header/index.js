import React from 'react';

/**
* @author
* @function Header
**/

const Header = (props) => {
  return(
    <header>
        <nav>
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Contact</a>
        </nav>
    </header>
   )

 }

export default Header

